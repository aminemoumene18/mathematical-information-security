# РОССИЙСКИЙ УНИВЕРСИТЕТ ДРУЖБЫ НАРОДОВ

Факультет физико-математических и естественных наук

Кафедра прикладной информатики и теории вероятностей

ОТЧЕТ

по лабораторной работе № 1

дисциплина: Математические основы защиты информации и информационной безопасности

Студент: Мумэн Амин

Группа: НФИмд-01-25

МОСКВА 2026 г.


Цель работы

Изучить исторические примеры шифров подстановки (на примере шифров Юлия Цезаря и императора Августа).

Реализовать их программно на языке Python.

Проверить корректность работы на исторических примерах


Теоретическая часть:

Шифр Цезаря — каждый символ открытого текста заменяется символом, находящимся на фиксированное число позиций k правее в алфавите. Математическая модель:

E(i) = (i + k) mod 26

где i — номер буквы в алфавите (A=0, B=1, ..., Z=25), k — ключ.

Шифр Атбаш — зеркальная замена: первая буква алфавита меняется с последней, вторая — с предпоследней и т.д.

E(i) = 25 - i


Программная реализация

Инструменты: Python 3, модуль string.
Алфавит: заглавные латинские буквы A-Z (для Атбаш добавлен пробел).

3.1 Функция шифра Цезаря

import string

alphabet = list(string.ascii_uppercase)

def cesar_cipher(k, text):
    result = ""
    for letter in text:
        idx = alphabet.index(letter)
        result += alphabet[(idx + k) % 26]
    return result

3.2 Функция шифра Атбаш

alphabet = list(string.ascii_uppercase) + [" "]

def atbash_cipher(text):
    indexes = [alphabet.index(ch) for ch in text]
    reversed_alphabet = list(reversed(alphabet))
    return "".join(reversed_alphabet[i] for i in indexes)

Исправления, внесённые в исходный код:

- Убрано глобальное изменение алфавита (reversed_alphabet создаётся локально).

- Добавлен оператор return в функцию atbash_cipher.

Результаты тестирования

Шифр     Входной текст     Ключ     Ожидаемый результат     Полученный результат
Цезарь   YZZ               3        BCC                      BCC
Атбаш    EEE AF            ---      VVH ZU                   VVH ZU

Все результаты совпали с ожидаемыми, алгоритмы работают корректно.

Выводы

1. Реализованы два классических шифра подстановки.

2. Освоена математическая модель модульного сдвига.

3.Исправлены ошибки в предоставленном коде (побочное изменение глобальных данных, отсутствие return).

4. Программы протестированы и готовы к использованию.
